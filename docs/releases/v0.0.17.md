# Codex Window Reset v0.0.17

修复 v0.0.16 发布门禁：真实 Go 嵌入页面已使用 18:00 非对齐下班时间验证三次预热，源码浏览器审计样例恢复其独立的 19:00 时间轴基准，避免测试数据互相污染。

v0.0.16 的失败原因已记录：Release/CI 的 `audit-axis` 仍断言 05:00–20:00 轴，但静态样例被错误改为 18:00，导致轴域断言失败；不是调度逻辑或环境故障。失败标签保留，v0.0.17 使用新标签发布。
