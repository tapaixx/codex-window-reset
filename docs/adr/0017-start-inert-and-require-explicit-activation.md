# Start inert and require explicit activation

A new installation starts with scheduling disabled and no Scheduled Accounts. The interface may suggest a default timezone of `Asia/Shanghai` and example Critical Work Periods, but automatic requests cannot begin until the Operator selects accounts, supplies the required Preheat Window parameters, saves a valid configuration, and explicitly enables the schedule.
