# Schedule one request inside a preheat window

Each Critical Work Period derives a Preheat Window that ends a configurable lead time before work begins and extends backward for a configurable span. Every eligible Scheduled Account executes at most one Preheat Request inside that interval, allowing account staggering without turning “preheat duration” into repeated traffic.
